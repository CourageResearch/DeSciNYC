/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'i3.ytimg.com'
        ],
    },
};

export default nextConfig;
